mysql -u root -p -e "
    CREATE DATABASE IF NOT EXISTS utm_test; 
    GRANT ALL PRIVILEGES ON utm_test.* TO 'utm'@'localhost';
    FLUSH PRIVILEGES;
    select host,db,user from mysql.db where user='utm';"